package com.campusvirtual.model.enums;

/**
 * Tipos de cuenta disponibles en el sistema.
 * Coinciden con los valores ENUM de la columna 'role' en la tabla users.
 */
public enum AccountType {
    STUDENT,
    PROFESSOR,
    ADMIN
}
