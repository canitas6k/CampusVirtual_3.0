# =============================================================
# Configuración de Base de Datos - Campus Virtual 3.0
# =============================================================
# Copia este fichero como "db.properties" y rellena los valores.
# NUNCA subas db.properties a Git (está en .gitignore).
# =============================================================

db.url=jdbc:mysql://localhost:3306/campus_virtual
db.user=root
db.password=
