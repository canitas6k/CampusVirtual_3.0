package com.campusvirtual.web.entity;

/**
 * Roles disponibles en el sistema.
 * Coinciden con los valores ENUM de la columna 'role' en MySQL.
 */
public enum Role {
    STUDENT,
    PROFESSOR,
    ADMIN
}
